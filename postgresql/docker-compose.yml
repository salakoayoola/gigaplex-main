services:
  db:
    image: postgres:16-alpine
    restart: always
    container_name: postgres_db_default
    environment:
      - POSTGRES_USER=${USERNAME}
      - POSTGRES_PASSWORD=${PASSWORD}
    ports:
      - 5432:5432
    volumes:
      - /mnt/homeserver-config/postgres/data:/var/lib/postgresql/data
    networks:
      - postgres
      - backend
  
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: postgres_pgadmin
    restart: always
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
      - ALLOW_SPECIAL_EMAIL_DOMAINS=True
    ports:
      - 8080:80
    networks:
      - postgres
      - frontend
      - backend
# db-backup:
#     image: rclone/rclone:latest
#     container_name: postgres_db_backup
#     restart: unless-stopped
#     privileged: true
#     security_opt:
#       - apparmor:unconfined
#     cap_add:
#       - SYS_ADMIN
#     volumes:
#       - /mnt/homeserver-config/postgres/backups:/backups
#       - /mnt/homeserver-config/rclone:/config/rclone  # rclone config dir
#     environment:
#       - PGPASSWORD=${PASSWORD}
#       - PHP_TZ=Europe/London
#     entrypoint: /bin/sh -c
#     command: |
#       "
#       while true; do
#         TIMESTAMP=$(date +%Y%m%d-%H%M%S)
#         pg_dump -h db -U ${USERNAME} --format=custom --file=/backups/backup-$TIMESTAMP.dump
#         rclone copy /backups/backup-$TIMESTAMP.dump postgres-backblaze:postgres-backups
#         sleep 86400
#       done
#       "
#     depends_on:
#       - db
#     networks:
#       - postgres

networks:
  postgres:
    external: true
  backend:
    external: true
  frontend:
    external: true