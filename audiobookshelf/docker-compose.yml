services:
  audiobookshelf:
    image: ghcr.io/advplyr/audiobookshelf:latest
    ports:
      - 13378:80
    volumes:
      - ${AUDIOBOOKS}:/audiobooks
      - ${PODCASTS}:/podcasts
      - ./config:/config
      - ./metadata:/metadata
    environment:
      - TZ=Europe/London